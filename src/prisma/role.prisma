model Role {
  id                Int          @id @default(autoincrement())
  createdAt         DateTime     @default(now())
  name              String
  isDefault         Boolean      @default(false)
}